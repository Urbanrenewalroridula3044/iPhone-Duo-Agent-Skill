# FloatingDock

- `DockBleedModifier` reads the interface orientation deliberately: it is the only way to know which end of a
  landscape iPhone has the cutout. Do not replace it with size classes or an aspect-ratio check.
